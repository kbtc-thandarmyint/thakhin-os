#!/bin/sh
# Activate the Thakhin plymouth boot splash
set -e

if command -v plymouth-set-default-theme >/dev/null 2>&1; then
  plymouth-set-default-theme thakhin || echo "WARN: could not set plymouth theme"
  update-initramfs -u || echo "WARN: initramfs update failed"
fi
